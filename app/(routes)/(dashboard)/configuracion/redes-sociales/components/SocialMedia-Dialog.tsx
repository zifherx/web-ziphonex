"use client";

import { SOCIALMEDIA_DIALOG_PROP } from "@/common/types/socialmedia-props";

export function SocialMediaDialog({
  editItem,
  onOpenChange,
  onSubmit,
  open,
  isSubmitting = false,
}: SOCIALMEDIA_DIALOG_PROP) {
  return <div>SocialMediaDialog</div>;
}
